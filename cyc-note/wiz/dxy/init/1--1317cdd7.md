---
Title: "1"
Url: ""
Author: ""
Origin: "WizNote"
Description: ""
Tags: []
Created: "2023-06-19 22:12:28"
Cover: ""
WizGuid: "1317cdd7-202b-4d4b-9fc4-d304c2adce4c"
WizType: "document"
WizLocation: "/dxy/init/"
WizDataMd5: "8d49042d357eb5c76154e34962f8f131"
Modified: "2023-06-19 22:15:40"
WizSyncedAt: "2026-07-29 15:36:28"
---

select b.id, b.question_id, q.question_type, q.question_year, q.cate_no, b.cate_no_ext, b.position, b.body_status from question as q join question_body as b on q.id = b.question_id where q.is_deleted = 0 and b.is_deleted = 0 and q.question_status = 1 and b.body_status = 1 and q.category_type = 1 and q.basic_question = true and q.question_year <> '' and (q.cate_no like '0990403050006%' or q.exam_type =9 )

```

```

17

1

```
select
```

2

```
        b.id,
```

3

```
        b.question_id,
```

4

```
        q.question_type,
```

5

```
        q.question_year,
```

6

```
        q.cate_no,
```

7

```
        b.cate_no_ext,
```

8

```
        b.position,
```

9

```
        b.body_status
```

10

```
        from question as q join question_body as b on q.id = b.question_id
```

11

```
        where
```

12

```
        q.is_deleted = 0 and b.is_deleted = 0
```

13

```
        and q.question_status = 1 and b.body_status = 1
```

14

```
        and q.category_type = 1
```

15

```
        and q.basic_question = true
```

16

```
        and q.question_year  <>  ''
```

17

```
        and (q.cate_no like '0990403050006%' or q.exam_type =9 )
```

select b.id, b.question_id, q.question_type, q.question_year, q.cate_no, b.cate_no_ext, b.position, b.body_status from question as q join question_body as b on q.id = b.question_id where q.is_deleted = 0 and b.is_deleted = 0 and q.question_status = 1 and b.body_status = 1 and q.category_type = 1 and q.basic_question = true and q.question_year <> '' and (q.cate_no = '0990403050006' or q.exam_type =9 )

```

```

17

1

```
select
```

2

```
        b.id,
```

3

```
        b.question_id,
```

4

```
        q.question_type,
```

5

```
        q.question_year,
```

6

```
        q.cate_no,
```

7

```
        b.cate_no_ext,
```

8

```
        b.position,
```

9

```
        b.body_status
```

10

```
        from question as q join question_body as b on q.id = b.question_id
```

11

```
        where
```

12

```
        q.is_deleted = 0 and b.is_deleted = 0
```

13

```
        and q.question_status = 1 and b.body_status = 1
```

14

```
        and q.category_type = 1
```

15

```
        and q.basic_question = true
```

16

```
        and q.question_year  <>  ''
```

17

```
        and (q.cate_no = '0990403050006' or q.exam_type =9 )
```
