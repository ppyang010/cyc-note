---
Title: "无标题 (3)"
Url: ""
Author: ""
Origin: "WizNote"
Description: ""
Tags: []
Created: "2020-03-20 19:24:34"
Cover: ""
WizGuid: "82f9e59a-36c6-4f88-b049-97381550006f"
WizType: ""
WizLocation: "/dxy/init/"
WizDataMd5: "257fdb4f958e43587bf8a9a62c7c2d32"
Modified: "2020-03-20 19:24:45"
WizSyncedAt: "2026-07-29 15:36:28"
---

create table lecturer_revenue_sharing_manage ( id int unsigned auto_increment comment '自增id', goods_id int unsigned default 0 not null comment '商品id', goods_type tinyint default 0 not null comment '商品类型', type_and_id varchar(20) default '' not null comment '商品类型_商品ID(方便查询)', lecturer_name varchar(100) default '' not null comment '讲师名称', deadline_time datetime default current_timestamp not null comment '截止时间', settlement_cycle_type tinyint default 0 not null comment '结算周期,0未设置,1月度,2季度', amount_type tinyint default 0 not null comment '金额类型,0未设置,1税前,2税后', account_type tinyint default 0 not null comment '打款账号类型,0未设置,1对公,2对私', lecturer_chance_key varchar(50) default '' not null comment '讲师机会key', revenue_sharing_type tinyint default 0 not null comment '分成方式类型;0未设置;1:销售额比例;2:利润比例;3:净利润比例;4:买断分成;5完结分成1;6完结分成2;7科研会员', revenue_sharing_value int default 0 not null comment '分成比例/课时单价', created_time datetime default current_timestamp not null comment '创建时间', modified_time datetime default current_timestamp not null on update current_timestamp comment '修改时间', is_deleted tinyint default 0 not null comment '逻辑删除 0：正常 1：已删除', primary key (`id`), key `idx_goods_id` (`goods_id`, `goods_type`), key `idx_type_and_id` (`type_and_id`) ) default charset = utf8mb4 comment ='讲师分成设置管理'; create table lecturer_revenue_sharing_manage_history ( id int unsigned auto_increment comment '自增id', source_id int unsigned default 0 not null comment '原数据id', goods_id int unsigned default 0 not null comment '商品id', goods_type tinyint default 0 not null comment '商品类型', type_and_id varchar(20) default '' not null comment '商品类型_商品ID(方便查询)', lecturer_name varchar(100) default '' not null comment '讲师名称', deadline_time datetime default current_timestamp not null comment '截止时间', settlement_cycle_type tinyint default 0 not null comment '结算周期,0未设置,1月度,2季度', amount_type tinyint default 0 not null comment '金额类型,0未设置,1税前,2税后', account_type tinyint default 0 not null comment '打款账号类型,0未设置,1对公,2对私', lecturer_chance_key varchar(50) default '' not null comment '讲师机会key', revenue_sharing_type tinyint default 0 not null comment '分成方式类型;0未设置;1:销售额比例;2:利润比例;3:净利润比例;4:买断分成;5完结分成1;6完结分成2;7科研会员', revenue_sharing_value int default 0 not null comment '分成比例/课时单价', created_time datetime default current_timestamp not null comment '创建时间', modified_time datetime default current_timestamp not null on update current_timestamp comment '修改时间', is_deleted tinyint default 0 not null comment '逻辑删除 0：正常 1：已删除', primary key (`id`), key `idx_goods_id` (`goods_id`, `goods_type`), key `idx_type_and_id` (`type_and_id`) ) default charset = utf8mb4 comment ='讲师分成设置管理操作历史'; create table course_sales_flow ( id int unsigned auto_increment comment '自增id', goods_id int unsigned default 0 not null comment '商品id', goods_type tinyint default 0 not null comment '商品类型', type_and_id varchar(20) default '' not null comment '商品类型_商品ID(方便查询)', year int default 0 not null comment '流水年份', month int default 0 not null comment '流水月份', wechat_income int default 0 not null comment '微信端收入流水(单位:分)', alipay_income int default 0 not null comment '支付宝端收入流失(单位:分)', apple_income int default 0 not null comment '苹果端收入流水(单位:分)', other_income int default 0 not null comment '其他收入流水(单位:分)', wechat_service_charge int default 0 not null comment '微信端服务费(单位:分)', alipay_service_charge int default 0 not null comment '支付宝服务费(单位:分)', apple_service_charge int default 0 not null comment '苹果服务费(单位:分)', other_charge int default 0 not null comment '其他费用(单位:分)', tax int default 0 not null comment '税费(单位:分)', operating_cost int default 0 not null comment '运营成本(单位:分)', created_time datetime default current_timestamp not null comment '创建时间', modified_time datetime default current_timestamp not null on update current_timestamp comment '修改时间', is_deleted tinyint default 0 not null comment '逻辑删除 0：正常 1：已删除', primary key (`id`), key `idx_goods_id` (`goods_id`, `goods_type`), key `idx_type_and_id` (`type_and_id`) ) default charset = utf8mb4 comment = '课程销售流水表'; create table lecturer_revenue_sharing_detail ( id int unsigned auto_increment comment '自增id', goods_id int unsigned default 0 not null comment '商品id', goods_type tinyint default 0 not null comment '商品类型', type_and_id varchar(20) default '' not null comment '商品类型_商品ID(方便查询)', lecturer_name varchar(100) default '' not null comment '讲师名称', deadline_time datetime default current_timestamp not null comment '截止时间', settlement_cycle_type tinyint default 0 not null comment '结算周期,0未设置,1月度,2季度', amount_type tinyint default 0 not null comment '金额类型,0未设置,1税前,2税后', account_type tinyint default 0 not null comment '打款账号类型,0未设置,1对公,2对私', lecturer_chance_key varchar(50) default '' not null comment '讲师机会key', revenue_sharing_type tinyint default 0 not null comment '分成方式类型;0未设置;1:销售额比例;2:利润比例;3:净利润比例;4:买断分成;5完结分成1;6完结分成2;7科研会员', revenue_sharing_value int default 0 not null comment '分成比例/课时单价', year int default 0 not null comment '年份', month int default 0 not null comment '月份', course_sales int default 0 not null comment '周期内课程销售额(单位:分)', hour_update_num int default 0 not null comment '周期内课时更新数', total_pay_amount int default 0 not null comment '应发分成金额(单位:分)', paid_amount int default 0 not null comment '本月发放金额(单位:分)', unpaid_amount int default 0 not null comment '本月结余金额(单位:分)', rel_payment_record int default 0 not null comment '关联讲师发放记录表id', created_time datetime default current_timestamp not null comment '创建时间', modified_time datetime default current_timestamp not null on update current_timestamp comment '修改时间', is_deleted tinyint default 0 not null comment '逻辑删除 0：正常 1：已删除', primary key (`id`), key `idx_goods_id` (`goods_id`, `goods_type`), key `idx_type_and_id` (`type_and_id`), key `idx_type_and_id` (`type_and_id`) ) default charset = utf8mb4 comment ='讲师分成明细表'; create table lecturer_revenue_sharing_detail_history ( id int unsigned auto_increment comment '自增id', source_id int unsigned default 0 not null comment '原数据id', goods_id int unsigned default 0 not null comment '商品id', goods_type tinyint default 0 not null comment '商品类型', type_and_id varchar(20) default '' not null comment '商品类型_商品ID(方便查询)', lecturer_name varchar(100) default '' not null comment '讲师名称', deadline_time datetime default current_timestamp not null comment '截止时间', settlement_cycle_type tinyint default 0 not null comment '结算周期,0未设置,1月度,2季度', amount_type tinyint default 0 not null comment '金额类型,0未设置,1税前,2税后', account_type tinyint default 0 not null comment '打款账号类型,0未设置,1对公,2对私', lecturer_chance_key varchar(50) default '' not null comment '讲师机会key', revenue_sharing_type tinyint default 0 not null comment '分成方式类型;0未设置;1:销售额比例;2:利润比例;3:净利润比例;4:买断分成;5完结分成1;6完结分成2;7科研会员', revenue_sharing_value int default 0 not null comment '分成比例/课时单价', year int default 0 not null comment '年份', month int default 0 not null comment '月份', course_sales int default 0 not null comment '周期内课程销售额(单位:分)', hour_update_num int default 0 not null comment '周期内课时更新数', total_pay_amount int default 0 not null comment '应发分成金额(单位:分)', paid_amount int default 0 not null comment '本月发放金额(单位:分)', unpaid_amount int default 0 not null comment '本月结余金额(单位:分)', created_time datetime default current_timestamp not null comment '创建时间', modified_time datetime default current_timestamp not null on update current_timestamp comment '修改时间', is_deleted tinyint default 0 not null comment '逻辑删除 0：正常 1：已删除', primary key (`id`), key `idx_goods_id` (`goods_id`, `goods_type`), key `idx_type_and_id` (`type_and_id`), key `idx_type_and_id` (`type_and_id`) ) default charset = utf8mb4 comment ='讲师分成明细操作历史表'; create table lecturer_revenue_sharing_payment_record ( id int unsigned auto_increment comment '自增id', goods_id int unsigned default 0 not null comment '商品id', goods_type tinyint default 0 not null comment '商品类型', type_and_id varchar(20) default '' not null comment '商品类型_商品ID(方便查询)', lecturer_name varchar(100) default '' not null comment '讲师名称', settlement_cycle_type tinyint default 0 not null comment '结算周期,0未设置,1月度,2季度', year int default 0 not null comment '年份', month int default 0 not null comment '月份', paid_amount int default 0 not null comment '本月发放金额(单位:分)', paid_status int default 0 not null comment '发款状态,0未发放,1发放中,2已发放', created_time datetime default current_timestamp not null comment '创建时间', modified_time datetime default current_timestamp not null on update current_timestamp comment '修改时间', is_deleted tinyint default 0 not null comment '逻辑删除 0：正常 1：已删除', primary key (`id`), key `idx_goods_id` (`goods_id`, `goods_type`), key `idx_type_and_id` (`type_and_id`), key `idx_type_and_id` (`type_and_id`) ) default charset = utf8mb4 comment ='讲师分成收入发放记录表'; create table lecturer_revenue_sharing_payment_statistics ( id int unsigned auto_increment comment '自增id', year int default 0 not null comment '年份', month int default 0 not null comment '月份', clinical_paid_amount int default 0 not null comment '临床分类发放金额(单位:分)', exam_paid_amount int default 0 not null comment '医考分类发放金额(单位:分)', sci_paid_amount int default 0 not null comment '医考分类发放金额(单位:分)', created_time datetime default current_timestamp not null comment '创建时间', modified_time datetime default current_timestamp not null on update current_timestamp comment '修改时间', is_deleted tinyint default 0 not null comment '逻辑删除 0：正常 1：已删除', primary key (`id`) ) default charset = utf8mb4 comment ='讲师分成发放各类目统计表';

194

1

```
create table lecturer_revenue_sharing_manage
```

2

```
(
```

3

```
    id                    int unsigned auto_increment comment '自增id',
```

4

```
    goods_id              int unsigned default 0                 not null comment '商品id',
```

5

```
    goods_type            tinyint      default 0                 not null comment '商品类型',
```

6

```
    type_and_id           varchar(20)  default ''                not null comment '商品类型_商品ID(方便查询)',
```

7

```
    lecturer_name         varchar(100) default ''                not null comment '讲师名称',
```

8

```
    deadline_time         datetime     default current_timestamp not null comment '截止时间',
```

9

```

```

10

```
    settlement_cycle_type tinyint      default 0                 not null comment '结算周期,0未设置,1月度,2季度',
```

11

```
    amount_type           tinyint      default 0                 not null comment '金额类型,0未设置,1税前,2税后',
```

12

```
    account_type          tinyint      default 0                 not null comment '打款账号类型,0未设置,1对公,2对私',
```

13

```
    lecturer_chance_key   varchar(50)  default ''                not null comment '讲师机会key',
```

14

```
    revenue_sharing_type  tinyint      default 0                 not null comment '分成方式类型;0未设置;1:销售额比例;2:利润比例;3:净利润比例;4:买断分成;5完结分成1;6完结分成2;7科研会员',
```

15

```
    revenue_sharing_value int          default 0                 not null comment '分成比例/课时单价',
```

16

```

```

17

```
    created_time          datetime     default current_timestamp not null comment '创建时间',
```

18

```
    modified_time         datetime     default current_timestamp not null on update current_timestamp comment '修改时间',
```

19

```
    is_deleted            tinyint      default 0                 not null comment '逻辑删除 0：正常 1：已删除',
```

20

```
    primary key (`id`),
```

21

```
    key `idx_goods_id` (`goods_id`, `goods_type`),
```

22

```
    key `idx_type_and_id` (`type_and_id`)
```

23

```

```

24

```
) default charset = utf8mb4 comment ='讲师分成设置管理';
```

25

```

```

26

```

```

27

```
create table lecturer_revenue_sharing_manage_history
```

28

```
(
```

29

```
    id                    int unsigned auto_increment comment '自增id',
```

30

```
    source_id             int unsigned default 0                 not null comment '原数据id',
```

31

```
    goods_id              int unsigned default 0                 not null comment '商品id',
```

32

```
    goods_type            tinyint      default 0                 not null comment '商品类型',
```

33

```
    type_and_id           varchar(20)  default ''                not null comment '商品类型_商品ID(方便查询)',
```

34

```
    lecturer_name         varchar(100) default ''                not null comment '讲师名称',
```

35

```
    deadline_time         datetime     default current_timestamp not null comment '截止时间',
```

36

```

```

37

```
    settlement_cycle_type tinyint      default 0                 not null comment '结算周期,0未设置,1月度,2季度',
```

38

```
    amount_type           tinyint      default 0                 not null comment '金额类型,0未设置,1税前,2税后',
```

39

```
    account_type          tinyint      default 0                 not null comment '打款账号类型,0未设置,1对公,2对私',
```

40

```
    lecturer_chance_key   varchar(50)  default ''                not null comment '讲师机会key',
```

41

```
    revenue_sharing_type  tinyint      default 0                 not null comment '分成方式类型;0未设置;1:销售额比例;2:利润比例;3:净利润比例;4:买断分成;5完结分成1;6完结分成2;7科研会员',
```

42

```
    revenue_sharing_value int          default 0                 not null comment '分成比例/课时单价',
```

43

```

```

44

```
    created_time          datetime     default current_timestamp not null comment '创建时间',
```

45

```
    modified_time         datetime     default current_timestamp not null on update current_timestamp comment '修改时间',
```

46

```
    is_deleted            tinyint      default 0                 not null comment '逻辑删除 0：正常 1：已删除',
```

47

```
    primary key (`id`),
```

48

```
    key `idx_goods_id` (`goods_id`, `goods_type`),
```

49

```
    key `idx_type_and_id` (`type_and_id`)
```

50

```

```

51

```
) default charset = utf8mb4 comment ='讲师分成设置管理操作历史';
```

52

```

```

53

```

```

54

```

```

55

```
create table course_sales_flow
```

56

```
(
```

57

```
    id                    int unsigned auto_increment comment '自增id',
```

58

```
    goods_id              int unsigned default 0                 not null comment '商品id',
```

59

```
    goods_type            tinyint      default 0                 not null comment '商品类型',
```

60

```
    type_and_id           varchar(20)  default ''                not null comment '商品类型_商品ID(方便查询)',
```

61

```
    year                  int          default 0                 not null comment '流水年份',
```

62

```
    month                 int          default 0                 not null comment '流水月份',
```

63

```
    wechat_income         int          default 0                 not null comment '微信端收入流水(单位:分)',
```

64

```
    alipay_income         int          default 0                 not null comment '支付宝端收入流失(单位:分)',
```

65

```
    apple_income          int          default 0                 not null comment '苹果端收入流水(单位:分)',
```

66

```
    other_income          int          default 0                 not null comment '其他收入流水(单位:分)',
```

67

```
    wechat_service_charge int          default 0                 not null comment '微信端服务费(单位:分)',
```

68

```
    alipay_service_charge int          default 0                 not null comment '支付宝服务费(单位:分)',
```

69

```
    apple_service_charge  int          default 0                 not null comment '苹果服务费(单位:分)',
```

70

```
    other_charge          int          default 0                 not null comment '其他费用(单位:分)',
```

71

```
    tax                   int          default 0                 not null comment '税费(单位:分)',
```

72

```
    operating_cost        int          default 0                 not null comment '运营成本(单位:分)',
```

73

```

```

74

```
    created_time          datetime     default current_timestamp not null comment '创建时间',
```

75

```
    modified_time         datetime     default current_timestamp not null on update current_timestamp comment '修改时间',
```

76

```
    is_deleted            tinyint      default 0                 not null comment '逻辑删除 0：正常 1：已删除',
```

77

```
    primary key (`id`),
```

78

```
    key `idx_goods_id` (`goods_id`, `goods_type`),
```

79

```
    key `idx_type_and_id` (`type_and_id`)
```

80

```

```

81

```
) default charset = utf8mb4 comment = '课程销售流水表';
```

82

```

```

83

```

```

84

```
create table lecturer_revenue_sharing_detail
```

85

```
(
```

86

```
    id                    int unsigned auto_increment comment '自增id',
```

87

```
    goods_id              int unsigned default 0                 not null comment '商品id',
```

88

```
    goods_type            tinyint      default 0                 not null comment '商品类型',
```

89

```
    type_and_id           varchar(20)  default ''                not null comment '商品类型_商品ID(方便查询)',
```

90

```
    lecturer_name         varchar(100) default ''                not null comment '讲师名称',
```

91

```

```

92

```
    deadline_time         datetime     default current_timestamp not null comment '截止时间',
```

93

```
    settlement_cycle_type tinyint      default 0                 not null comment '结算周期,0未设置,1月度,2季度',
```

94

```
    amount_type           tinyint      default 0                 not null comment '金额类型,0未设置,1税前,2税后',
```

95

```
    account_type          tinyint      default 0                 not null comment '打款账号类型,0未设置,1对公,2对私',
```

96

```
    lecturer_chance_key   varchar(50)  default ''                not null comment '讲师机会key',
```

97

```
    revenue_sharing_type  tinyint      default 0                 not null comment '分成方式类型;0未设置;1:销售额比例;2:利润比例;3:净利润比例;4:买断分成;5完结分成1;6完结分成2;7科研会员',
```

98

```
    revenue_sharing_value int          default 0                 not null comment '分成比例/课时单价',
```

99

```

```

100

```
    year                  int          default 0                 not null comment '年份',
```

101

```
    month                 int          default 0                 not null comment '月份',
```

102

```
    course_sales          int          default 0                 not null comment '周期内课程销售额(单位:分)',
```

103

```
    hour_update_num       int          default 0                 not null comment '周期内课时更新数',
```

104

```
    total_pay_amount      int          default 0                 not null comment '应发分成金额(单位:分)',
```

105

```
    paid_amount           int          default 0                 not null comment '本月发放金额(单位:分)',
```

106

```
    unpaid_amount         int          default 0                 not null comment '本月结余金额(单位:分)',
```

107

```

```

108

```
    rel_payment_record    int          default 0                 not null comment '关联讲师发放记录表id',
```

109

```

```

110

```
    created_time          datetime     default current_timestamp not null comment '创建时间',
```

111

```
    modified_time         datetime     default current_timestamp not null on update current_timestamp comment '修改时间',
```

112

```
    is_deleted            tinyint      default 0                 not null comment '逻辑删除 0：正常 1：已删除',
```

113

```
    primary key (`id`),
```

114

```
    key `idx_goods_id` (`goods_id`, `goods_type`),
```

115

```
    key `idx_type_and_id` (`type_and_id`),
```

116

```
    key `idx_type_and_id` (`type_and_id`)
```

117

```

```

118

```
) default charset = utf8mb4 comment ='讲师分成明细表';
```

119

```

```

120

```

```

121

```
create table lecturer_revenue_sharing_detail_history
```

122

```
(
```

123

```
    id                    int unsigned auto_increment comment '自增id',
```

124

```
    source_id             int unsigned default 0                 not null comment '原数据id',
```

125

```
    goods_id              int unsigned default 0                 not null comment '商品id',
```

126

```
    goods_type            tinyint      default 0                 not null comment '商品类型',
```

127

```
    type_and_id           varchar(20)  default ''                not null comment '商品类型_商品ID(方便查询)',
```

128

```
    lecturer_name         varchar(100) default ''                not null comment '讲师名称',
```

129

```

```

130

```
    deadline_time         datetime     default current_timestamp not null comment '截止时间',
```

131

```
    settlement_cycle_type tinyint      default 0                 not null comment '结算周期,0未设置,1月度,2季度',
```

132

```
    amount_type           tinyint      default 0                 not null comment '金额类型,0未设置,1税前,2税后',
```

133

```
    account_type          tinyint      default 0                 not null comment '打款账号类型,0未设置,1对公,2对私',
```

134

```
    lecturer_chance_key   varchar(50)  default ''                not null comment '讲师机会key',
```

135

```
    revenue_sharing_type  tinyint      default 0                 not null comment '分成方式类型;0未设置;1:销售额比例;2:利润比例;3:净利润比例;4:买断分成;5完结分成1;6完结分成2;7科研会员',
```

136

```
    revenue_sharing_value int          default 0                 not null comment '分成比例/课时单价',
```

137

```

```

138

```
    year                  int          default 0                 not null comment '年份',
```

139

```
    month                 int          default 0                 not null comment '月份',
```

140

```
    course_sales          int          default 0                 not null comment '周期内课程销售额(单位:分)',
```

141

```
    hour_update_num       int          default 0                 not null comment '周期内课时更新数',
```

142

```
    total_pay_amount      int          default 0                 not null comment '应发分成金额(单位:分)',
```

143

```
    paid_amount           int          default 0                 not null comment '本月发放金额(单位:分)',
```

144

```
    unpaid_amount         int          default 0                 not null comment '本月结余金额(单位:分)',
```

145

```
    created_time          datetime     default current_timestamp not null comment '创建时间',
```

146

```
    modified_time         datetime     default current_timestamp not null on update current_timestamp comment '修改时间',
```

147

```
    is_deleted            tinyint      default 0                 not null comment '逻辑删除 0：正常 1：已删除',
```

148

```
    primary key (`id`),
```

149

```
    key `idx_goods_id` (`goods_id`, `goods_type`),
```

150

```
    key `idx_type_and_id` (`type_and_id`),
```

151

```
    key `idx_type_and_id` (`type_and_id`)
```

152

```

```

153

```
) default charset = utf8mb4 comment ='讲师分成明细操作历史表';
```

154

```

```

155

```

```

156

```

```

157

```
create table lecturer_revenue_sharing_payment_record
```

158

```
(
```

159

```
    id                    int unsigned auto_increment comment '自增id',
```

160

```
    goods_id              int unsigned default 0                 not null comment '商品id',
```

161

```
    goods_type            tinyint      default 0                 not null comment '商品类型',
```

162

```
    type_and_id           varchar(20)  default ''                not null comment '商品类型_商品ID(方便查询)',
```

163

```
    lecturer_name         varchar(100) default ''                not null comment '讲师名称',
```

164

```
    settlement_cycle_type tinyint      default 0                 not null comment '结算周期,0未设置,1月度,2季度',
```

165

```
    year                  int          default 0                 not null comment '年份',
```

166

```
    month                 int          default 0                 not null comment '月份',
```

167

```
    paid_amount           int          default 0                 not null comment '本月发放金额(单位:分)',
```

168

```
    paid_status           int          default 0                 not null comment '发款状态,0未发放,1发放中,2已发放',
```

169

```
    created_time          datetime     default current_timestamp not null comment '创建时间',
```

170

```
    modified_time         datetime     default current_timestamp not null on update current_timestamp comment '修改时间',
```

171

```
    is_deleted            tinyint      default 0                 not null comment '逻辑删除 0：正常 1：已删除',
```

172

```
    primary key (`id`),
```

173

```

```

174

```
    key `idx_goods_id` (`goods_id`, `goods_type`),
```

175

```
    key `idx_type_and_id` (`type_and_id`),
```

176

```
    key `idx_type_and_id` (`type_and_id`)
```

177

```

```

178

```
) default charset = utf8mb4 comment ='讲师分成收入发放记录表';
```

179

```

```

180

```

```

181

```
create table lecturer_revenue_sharing_payment_statistics
```

182

```
(
```

183

```
    id                   int unsigned auto_increment comment '自增id',
```

184

```
    year                 int      default 0                 not null comment '年份',
```

185

```
    month                int      default 0                 not null comment '月份',
```

186

```
    clinical_paid_amount int      default 0                 not null comment '临床分类发放金额(单位:分)',
```

187

```
    exam_paid_amount     int      default 0                 not null comment '医考分类发放金额(单位:分)',
```

188

```
    sci_paid_amount      int      default 0                 not null comment '医考分类发放金额(单位:分)',
```

189

```
    created_time         datetime default current_timestamp not null comment '创建时间',
```

190

```
    modified_time        datetime default current_timestamp not null on update current_timestamp comment '修改时间',
```

191

```
    is_deleted           tinyint  default 0                 not null comment '逻辑删除 0：正常 1：已删除',
```

192

```
    primary key (`id`)
```

193

```

```

194

```
) default charset = utf8mb4 comment ='讲师分成发放各类目统计表';
```
