---
Title: "为什么现在 Agent 重新用回 Grep，而不是先做 RAG？ - Soulflare 的回答"
Url: "https://www.zhihu.com/question/2056838324170514762/answer/2080380982516102521"
Author: "Soulflare算法工程师​ 关注"
Origin: "知乎回答"
Description: "直接说结论：在真实的 Agent 工程落地里，用向量数据库做 RAG 很多时候不仅没有解决问题，反而成了系统里…"
Tags:
  - "grep"
  - "Agent"
  - "一起收藏人类美好"
Created: "2026-09-10 11:35:57"
Cover: "https://pica.zhimg.com/v2-5fc67a2efe2e8f52b40fac8a80da1442_l.jpg?source=2c26e567"
---

[让知识开源 · 科学季 2026](https://www.zhihu.com/parker/campaign/2078507658307543894?zh_hide_nav_bar=true)

49 人赞同了该回答

直接说结论：在真实的 Agent 工程落地里，用向量数据库做 RAG 很多时候不仅没有解决问题，反而成了系统里最脆弱、最不可控的一环。Agent 重新捡起 [Grep](https://zhida.zhihu.com/search?content_id=796965700&content_type=Answer&match_order=1&q=Grep&zhida_source=entity)，或者更准确地说是以 ripgrep、AST 语法树和精准路径匹配为代表的确定性文本检索，不是技术的倒退，而是工业界在交够了巨额学费之后的务实选择。

从 2023 年到 2024 年，整个行业几乎患上了向量依赖症。无论做什么私有化知识库还是代码助手，流程全是一套流水线：把文档或者代码库拉下来，按照五百个或者一千个 token 粗暴切开，调一个 embedding 接口存进向量数据库，检索时算余弦相似度，抓出来前五个片段塞给模型。当时大家觉得这是通用解法。

到了 2025 年下半年，尤其是 2026 年各类以实际任务完成率为导向的 Agent 跑进深水区之后，这套逻辑在工程层面全面暴露出严重水土不服。很多团队发现，辛辛苦苦搭起来的重型向量管线，在处理真实的排错、多文件代码修改以及复杂业务逻辑定位时，准确率经常被一个耗时只有十毫秒的 ripgrep 命令降维打击。

这背后的根本原因，不是算法工程师不够努力，而是向量语义检索从底层假设开始，就跟 Agent 的运行机理产生了错位。

### 语义相关性不等于逻辑精准性

向量检索的核心能力是计算语义层面的相近度。如果你的用户是一个普通的业务人员，记不清具体术语，模糊地问一句去年第三季度那个报销政策有什么变化，向量检索确实有用，因为它能理解用户的模糊意图，在海量非结构化文档里找到意思相近的段落。

但是 Agent 的工作场景完全不是这样。

Agent 面对的大多是高度严密、强逻辑依赖的环境。它要找的通常是一个具体的函数名、一个特定的错误状态码、一个配置文件里的键值、或者某一个精确调用的接口路径。比如 Agent 在调试一段逻辑，报错信息明确写着 invalid_token_handler。这时候，系统需要的是找到所有出现这个具体字符串的代码行。

如果走向量 RAG，灾难就发生了。向量模型对这类专有名词和符号是不敏感的。在 embedding 模型的潜空间里，用户鉴权失败、登录超时和 invalid_token_handler 可能会被算成极其相似的向量。结果就是，向量数据库热心肠地给模型召回了五篇关于登录机制的通用设计文档，甚至召回了另一个看起来功能类似但完全不相干的函数，偏偏把最关键的那一行真实定义漏掉了。

在开发 Agent 的过程中，我们最怕的不是检索不到，而是检索出一堆似是而非、看起来高度相关但实际上无法使用的信息。这种语义层面的漂移，会直接把模型带偏，让模型开始基于错误的上下文一本正经地编造解决方案。

而 Grep 没有任何理解能力，它只做一件事：确定性匹配。如果代码里有这个变量，它就会在一毫秒内把文件名、行号和上下文完整打在屏幕上；如果没有，就返回空。对于需要依靠事实采取下一步动作的 Agent 来说，这种非黑即白的确定性，价值远高于充满模糊幻觉的语义相似度。

### 动态环境下的索引维护成本是不可承受的

传统的 RAG 架构有一个极其致命的工程前提：数据是相对静态的。你先花几个小时建索引，然后系统开始对外提供只读检索。

可是 Agent 的本质是行动者，它要改写系统，要创建新文件，要修改现有代码，要在分支之间来回切换。当一个 Agent 在定位线上故障或者重构一个模块时，它可能在五分钟内编辑了八个文件，新增了两个调试类，删除了若干行调用。

如果依赖向量 RAG，这里就会面临一个死结：你要不要实时更新向量库？

如果你选择实时更新，那每一次磁盘写入，都得伴随着文本重切、调用 embedding 模型接口、重新写入向量库。这个过程存在不可避免的网络延迟和计算耗时。更别提由于切片边界处理不当，新增的一行代码可能会破坏原有 chunk 的上下文完整性。在几万个文件的复杂项目里，这种持续的向量重构能让系统资源直接枯竭，还伴随着严重的数据同步一致性问题。

如果你选择异步更新或者干脆不更新，那么在 Agent 进行第二步推理时，它向量库里拿到的数据就已经和磁盘上的真实状态脱节了。它检索出来的还是自己修改之前的旧代码，接着 Agent 就会依据失效的信息做决策，死循环就此产生。

我们之前在做企业级交付时，很多项目卡死在这里。后来我们干脆把代码仓和动态配置的向量索引全部砍掉，只留纯文本工具。关于这种从实验环境走向生产环境的残酷教训，业内踩坑的人非常多，感兴趣的可以去看这本 [大厂 Agent 落地手册：从 Demo 到生产避坑，10万字PDF](https://mp.weixin.qq.com/s/PksnfkyxXFD2mLrEzFFHkA?scene=1)，里面对生产环境下的技术选型和架构冗余有很详尽的复盘，基本把我们这两年踩过的泥潭都讲透了。

相比之下，Grep 工具是完全无状态的。它根本不需要在磁盘或者内存里维护一套额外的数据副本，不需要所谓的预处理，也不存在数据不同步的问题。Agent 刚写完一行代码，下一毫秒调用 ripgrep，就能在物理磁盘上扫出这一行。只要文件系统还在，检索的真实性就永远是百分之百的最新状态。这种零延迟的真实感，对于连续执行长程任务的 Agent 而言是保命的底线。

### 切片切断了代码与逻辑的拓扑结构

过去做 RAG 的人，几乎把全部精力都放在了切片参数调优上。有人主张五百字符加五十字符的滑动窗口，有人主张按换行符和段落切，有人尝试用小模型去辅助切片。

但是在真实的逻辑系统里，代码和强逻辑文本根本就不是线性排布的。一个类的定义可能跨越了两千行，最顶端是依赖引入，中间是成员变量，最末尾是关键的处理逻辑。当你用固定长度的窗口去切切片时，你几乎必然会在最关键的语义连接处一刀切断。

很多时候，函数的头部切在了上一个块，函数的报错返回切在了下一个块。单独把后半段抓出来给模型，模型连入参是什么、类型是什么、调用了哪些上下文变量都不知道。为了修补这个缺陷，人们又发明了父子文档检索、文档树展开、重排序等等极其繁复的管线。每加一层，系统的延迟就翻倍，故障率就成倍上升。

在 2026 年的今天，模型的上下文窗口早就不是当年那个 4k 或者 8k 的局促状态了。现在处理数十万 token 的长文本成本已经降得非常低，甚至模型的长程注意力召回能力也完全上了一个台阶。

既然长窗口已经普及，Agent 最理想的获取信息方式就已经变了。Agent 不需要人类事先自作聪明地把内容剁成肉馅。它要的是精准定位到具体位置，然后由它自己决定要把这整个文件拉出来看，还是只看这一行附近的五十行。

Grep 在这里扮演的正是这样一个轻量级的坐标发射器。Agent 执行一个简单的检索命令，拿到文件名和精确行号，接着调用读取工具，一口气把整个结构体或者整个模块完整读进上下文。上下文是浑然一体的，变量定义在，作用域在，引用关系全在。模型凭借自身越来越强大的长文本推理能力，能够一眼看清全局关系，而不是去拼凑五个散落各处的、被打碎了的向量切片。

### 单次召回与自主探索的交互范式差异

传统的 RAG 是为单轮问答设计的架构。用户提问，系统跑一次检索管线，把内容灌给大模型，大模型生成最终结果。这是一个单向的、被动的、一次性的管道。

但 Agent 的核心定义在于自主循环。Agent 的标准动作是：观察环境、提出假设、采取行动、评估工具返回的结果、修正假设、再采取下一个行动。

如果一个 Agent 的信息获取手段只有向量 RAG，它的探索链路其实是被锁死的。它发出一个查询，向量数据库依据某种算法直接塞给它一堆固定内容。如果这堆内容里没有答案，Agent 实际上很难修正自己的搜索方向，因为向量潜空间的距离计算对 Agent 来说是一个不可解释的黑盒，它不知道自己为什么搜不到，也不知道该怎么改写查询词才能避开那些噪声。

确定性的搜索工具赋予了 Agent 真正的试错空间。

一个成熟的代码或者运维 Agent，在实际排查问题时的行为轨迹是非常接近真实工程师的。它首先会用 ripgrep 搜一个核心关键词。如果命令行返回了三千条匹配项，Agent 看到终端输出就会立刻意识到：这个词太泛了。

于是它在下一步动作里，会自发加上文件后缀过滤，加上特定目录路径，甚至直接写一条正则表达式去收窄范围。如果搜出来的结果是零条，它就会换一个词，或者往上一层目录去找。在这个过程里，命令行的返回值、匹配数量、错误提示，全都是 Agent 调整策略的环境反馈。

确定性工具让 Agent 拥有了探索的支点。这种主动试探、逐步缩小的过程，完全符合大模型底层的链式推理逻辑。把一个黑盒的向量检索系统硬塞给 Agent，等于剥夺了它与环境底层真实结构互动的权利。

在这方面，全球范围内的技术演进路线其实是高度同步的。如果去观察 2025 到 2026 年这批真正跑出商业价值、能够稳定接管复杂任务的顶级团队，你会发现大家在底层的工具链设计上有着惊人的一致性。关于各大团队如何抛弃早期玩具式的 RAG 方案，转向更加确定、更加硬核的工程架构，建议研读一下这份 [全球大厂 Agent 落地实录（2025–2026）：37 家真实案例 + 12 条避坑清单](https://mp.weixin.qq.com/s/P5LG5t-xK29P7zcvVOA85w)，里面梳理的大量前沿工业实现，会让你少走大半年的弯路。

### 确定性与概率性的合理分工

我们在设计系统时，必须想明白一个常识：大语言模型本身就是一个极度复杂的概率预测系统。它给出的每句话、写出的每一行逻辑，本质上都带有随机性和不确定性。

如果在系统的信息输入端，你也采用一套基于向量距离打分的、充满各种相似度阈值妥协的概率系统，这就变成了概率套概率。输入是不确定的，召回的内容是不稳定的，中间的推理是发散的，最终整个 Agent 系统就会变得极其脆弱。任何一次参数的微调，甚至只是换了一个更长的提示词，都可能导致整条链条彻底坍塌。

一个健壮的工程系统，必须是用确定性的构件去约束概率性的核心。

让模型负责它最擅长的部分：理解高层次的逻辑、推导业务走向、规划行动步骤、评估环境状态。而在底层的数据读取、环境感知和事实检索上，必须毫不妥协地交还给确定性系统。

这也是为什么在代码和复杂知识领域，除了 Grep 之外，LSP 语言服务协议、AST 抽象语法树解析、基于符号表的直接跳转，正在成为 Agent 的标配。当 Agent 想知道一个方法在哪里被调用时，最有效的方式不是算余弦相似度，也不是单纯的文本匹配，而是直接向语言服务发一个查找所有引用的请求。这种基于编译器和文件系统的确定性反馈，能够给模型提供一块坚实的立足点。

### 这不是倒退，而是祛魅

很多技术人在刚接触新技术时，容易产生一种执念，觉得组件越新、链路越长、算法越复杂，系统的先进性就越高。向量数据库在过去几年里承担了太多它不该承担的期待。很多人以为给系统挂一个向量库，它就获得了记忆，就拥有了理解企业知识的能力。

真正到了工业交付那一天，面对苛刻的时延指标、极其有限的算力预算、零容忍的幻觉率要求，那些堆砌了大量中间件的架构往往率先崩溃。

这时候大家才回过神来。原来那个几十年前由 Unix 哲学孕育出来的、只有几千行底层 C 代码写就的文本查找逻辑，配合现代操作系统的页缓存和高度优化的正则引擎，不仅极度轻量、零外部依赖、响应时间在毫秒级别，而且给出的结果永远百分之百忠实于物理磁盘上的事实。

让大模型带着 ripgrep 和编译工具去探索世界，而不是把它关在向量检索过滤后的狭窄切片里。这才是这两年 Agent 架构演进过程中，最让人感到踏实的一个转变。技术没有高低贵贱，能够用最低的系统复杂度和最高的可靠性解决实际问题的手段，才是工业界最真实的选择。

还没有人送礼物，鼓励一下作者吧

[发布于2026-09-07 19:44](https://www.zhihu.com/question/2056838324170514762/answer/2080380982516102521) ・河北

[Qwen3.8-Max首发尝鲜，个企双版超值优惠低至39元/月起](https://click.aliyun.com/m/20000000945/?cb=https%3A%2F%2Fsugar.zhihu.com%2Fplutus_adreaper_callback%3Fsi%3Dfb7fae89-eb72-4b0f-bbc2-80d3e407402d%26os%3D3%26zid%3D1628%26zaid%3D3782464%26zcid%3D3799976%26cid%3D3799976%26event%3D__EVENTTYPE__%26value%3D__EVENTVALUE__%26score%3D__EVENTSCORE__%26ts%3D__TIMESTAMP__%26cts%3D__TS__%26mh%3D6f8f92f4fd73bb058d55979bdb706a03%26adv%3D645640%26ocg%3D0%26cp%3D0%26ocs%3D0%26aic%3D0%26atp%3D0%26ct%3D0%26ed%3DGiBNJgVzfCMmUW9XFyEvRA8xBGxJICwkOhh0FlwxKw1ZY0gnWzUoISkYdBZcPC1XVnUfO1UvKX1-AycSWDRxAV58CXoKdGlwdxVlVwJnfhYEK1wjXH5pd34SY1EDYWpcHngKfQh0a3Z8FHQVRDlxAVpxXHoPIjttL0c2XxxkKAhbaFsvCSJ3c3pBagICMS1WD3IIHK9rd6gDcD8%3D&spu=biz%3D0%26ci%3D3799976%26si%3D114e07aa-abd8-4d80-be1a-34d8e3aafd71%26ts%3D1789011348%26zid%3D1628)

Qwen3.8-Max 首发尝鲜、上新 deepseek-v4-flash，更多模态和旗舰模型共享额度，个企双版本超值优惠低至 39 元/...

![](https://pic1.zhimg.com/50/v2-8df2f61c4a5f9bbabaa71342f950921c_720w.jpg?source=1def8aca)

Claude Code 早期版本用的是 RAG 加本地向量库，后来 Anthropic 团队自己把这套删了，换成让模型直接调 grep 去代码库里搜。这一步反着来，恰好挑在所有人都不假思索的地方：过去两年，只要有人问"怎么做私有知识问答"，答案几乎是条件反射，建知识库，切 chunk，做 embedding，把相关片段召回来塞给模型。这套 RAG 流程已经变成默认动作，默认到没人再问一句"这一步到底有没有必要"。 结果做编码 Agent 的这拨人，反而把这一步整个拿掉了。我日常写代码主力就是 Claude Code，一开始也纳闷：一个能读百万 token 的模型，连向量检索都不给我上，就靠 grep 一遍遍扫，这不是开倒车吗？翻了一圈一手资料才发现，这不叫开倒车。代码这个场景本身就跟通用文档不一样，适合它的检索方式也不一样。今天就把这件事讲透。 先把话说清楚：这不是"RAG 已死" 网上现在一堆标题党，《The RAG Obituary》 1 这种直接给 RAG 发讣告的文章都登上 Hacker News 首页 2 了。我得先泼盆冷水，这个判断要克制。真实的情况是分场景的：在代码库这种结构化、符号精确、还随时在变的地方，让模型自己迭代着 grep，确实比预先建一套 embedding 索引更划算；但换到海量非结构化文档、纯语义概念检索那边，RAG 该有价值还是有价值，两条路现在甚至在往一起融。 所以这篇文章的落点是编码 Agent，而且只钻代码这一个场景。为什么 Claude Code、Codex CLI、Cline 这批工具在代码库里宁可 grep 也不做向量化，这个问题值得掰开看。至于什么是 RAG、什么是 embedding，这些基础概念官方文档 3 和一堆科普都写烂了，我给个链接你自己跳，这里不复读。 先看两条路长什么样。左边是 RAG 的预处理管线，一条线走到底，一次性把知识库嚼碎、编码、入库；右边是 agentic search，一个循环，模型自己搜、自己看、不够再搜。 证据链：是 Anthropic 自己下的这个判断 这事最有分量的地方在于它的来源。说这话的是 Claude Code 的创建者 Boris Cherny 本人，不是哪个外人在隔岸推测。早期版本确实上过 RAG，他在 X 上那条推文 4 里说得很直接，用过 RAG 加本地向量库，但后来发现下面这套更好用： agentic search generally works better... simpler and doesn't have the staleness issues. agentic search 普遍更好用，更简单，也没有索引过时那些破事。 更早在 2025 年 5 月的 Latent Space 播客 5 里，他和 Cat Wu 同台，被 swyx 问到代码检索怎么做，那句话我记到现在： we landed on just agentic search, which outperformed everything by a lot. 最后我们落到纯 agentic search 上，效果好得不止一点。这在当时挺意外的。 意外在哪？2024 年那会儿，大家默认 RAG 就是处理超出上下文那部分信息的标配。团队内部试过本地向量库、试过让模型递归式地自建索引，各种花活都玩过，最后留下来的就是最土的 glob 加 grep 由模型驱动。他在 Pragmatic Engineer 6 的访谈里还讲过灵感来源：在 Meta 时他见过工程师在 Instagram 代码库里找东西，当时内部 IDE 的跳转定义坏了，一群高级工程师就反复 grep 来回翻。一个人面对陌生代码库的真实行为，跟你想象里"打开向量索引精准召回"完全是两码事。 这里我得替他标一个诚实的注脚：Boris 也承认这个"赢了全场"的判断，一部分是靠团队内部的 vibe 加没公开的内部 benchmark，不是那种完全可复现的扎实指标。这句我照实转述，免得你觉得我在替谁背书。 关键是，这不是 Anthropic 一家的怪癖。Cline 7 把"No RAG, no embeddings, no vector databases"写成了宣言，Aider 8 用 tree-sitter 生成符号级 repo map，OpenAI 的 Codex CLI 9 关掉了社区提的向量索引请求，连老牌代码搜索厂 Sourcegraph 10 都把 embedding 暂时放一放了。这场集体撤退我在另一篇 《RAG 对 Agent 来说是不是必须的》 里逐家扫过一遍，还捎带看了 Coding Agent 之外的 Computer Use、Deep Research、知识问答那些类型，这里不重复。这篇只钻一个更细的问题： 同样是找代码，grep 到底是怎么、在什么条件下赢过 embedding 的。 六个理由，说白了都指向"代码不是普通文本" 第一个是精确性。代码要的是精确匹配，不是语义近似。 getUserById 这个函数，要么在文件里出现，要么不出现，没有"模糊命中"这一说。你上向量检索，它会把、 getUserByEmail getUserByName 这几个语义相邻的东西糊在一块儿给你，反而添乱。grep 一把下去，精确到行，你要的那个符号在哪一行，它就给你哪一行，不掺水。 第二个是实时性。文件系统本身就是一份永远最新的索引。Claude Code 的 Read 工具每次都对磁盘做一次 stat，没有缓存层，你改完一个文件 100 毫秒后问它，它读到的就是新字节。向量索引不行，它在下一次重新 embedding 之前一直是滞后的。Cline 那篇博客有个说法很到位：每一次 merge，都是现实和 AI 认知之间的一次潜在分叉。代码库天天在变，你那份索引凭什么永远对得上？ 第三个是维护成本，这也是我看到的最有说服力的一个模型。HarrisonSec 11 把 Agent 检索总结成一条成本曲线，用公式写出来是这样： 说白了，索引方案的 build（切块、编码、入库）和 maintain（失效、重建、跟代码漂移对账）这两项成本，会随着代码变更量线性甚至更快地往上爬；而 tool-loop 方案这两项直接是零，只剩每次查询那点多轮 LLM 往返的开销。画成图就很清楚了： 对中小型、日常都在改的代码库来说，那个 crossover 点你根本走不到，所以数学上算下来，答案就是 grep。这个视角我特别喜欢，它把一个看起来是"技术优劣"的问题，还原成了"成本结构"的问题。 第四个是上下文完整。切 chunk 这件事对代码是有伤害的。你按固定长度切，大概率把一个函数从中间劈开，把调用和定义分到两个 chunk 里，跨文件的依赖关系当场丢失。Cline 有个比喻我觉得挺形象：这就像听十秒一段的随机剪辑去理解一整首交响乐。agentic search 不切，模型要么读整个文件，要么读精确的行范围，拿到的永远是完整语义。 第五个是迭代式自我修正，这是 RAG 那种一次性召回给不了的。RAG 的流程是查询、召回、结束，错了就将错就错，你没有第二次机会。agentic search 是查询、看结果、发现不对、改写关键词、再来一次，模型能像人类工程师那样 grep 一下看看、跟着 import 往下摸、自己纠偏。Anthropic 在《Effective context engineering》 12 这篇里把这叫 just in time 检索，维护一些像文件路径这样的轻量标识符，运行时再用工具动态把数据捞进来，而不是一股脑预处理好全塞进去。 第六个是长上下文窗口。RAG 当年是怎么火起来的？是为了绕开 GPT-3.5 那 4096 token 的窗口，大概就六页纸，装不下只能检索。可现在窗口都到 200K 甚至上百万 token 了，模型能直接读大量原始内容，"信息塞不塞得进去"这个约束就松了。这里得补一句免得你误会：长上下文远没好用到能一吞了之，Chroma 的 Context Rot、NoLiMa、Lost in the Middle 那几组数据都说明窗口一长模型自己就先塌了，这块我在 那篇 里摆过实打实的数据。对代码场景来说重点在于，窗口够大之后，难点从"装不下"变成了"塞进去的东西是不是真的相关"，而 grep 恰好擅长只捞相关的那几行。这六个理由你连起来看，会发现它们其实是同一句话的六个侧面：代码不是普通文本，它精确、它多变、它有结构，所以适合它的检索方式，也跟通用文档不一样。 那 ripgrep 凭什么快到能当 Agent 的默认工具 既然全靠 grep 一遍遍扫，那这个 grep 的性能就得顶得住。这里得说说 ripgrep 13，也就是 rg，作者是 Andrew Gallant，网名 BurntSushi，用 Rust 写的，GitHub 上五万多星。它快在三个地方：SIMD 优化、并行遍历目录、还有一手"字面量提取"，先从你的正则里把固定的子串抠出来做快速字符串匹配，大部分文件在这一步就被跳过了，根本不用完整跑正则。 数字很能说明问题。按作者自己的 benchmark 14，在 Linux 内核树里搜一个模式，ripgrep 0.082 秒，GNU grep 0.671 秒，差着 八倍多；2026 年另一份实测 15 也复现了 5 到 13 倍这个区间。它对 Agent 还特别友好：默认递归、默认遵守.gitignore，自动跳过 node_modules.git、构建产物这些垃圾目录，还跳过隐藏文件和二进制文件，输出自带文件名和行号。这些默认行为，恰好就是一个 Agent 搜代码时想要的。 有一个被反复引用的数据点特别有意思：在十分钟的超时窗口里，一个 Agent 能跑大约 500 次 ripgrep 搜索，而用 GNU grep 只能跑大约 2 次。搜索几乎免费，这个假设才是 Claude Code 整个架构能成立的地基。也正因如此，Claude Code 的 Grep 工具本质就是套了一层 ripgrep，它的 system prompt 里甚至明文要求模型永远用内置的 Grep 工具、别去 Bash 里手敲。顺带一提，有人实测把它换成系统自带的 ripgrep 16，搜索还能再快上 5 到 10 倍，内置那份是为了跨平台兼容才打包的。这些工具怎么在 Agent 里被编排调用，我在之前那篇 《拆解 Claude Code》工具系统 里讲得更细，感兴趣可以顺着看。 倒是有个坑得提一句。ripgrep 也有性能悬崖。碰上百万文件级别的超大 monorepo，据分析，连 Cursor 那种优化过的即时 grep 都要十几秒，而它预建的索引方案零点零几秒就返回了，这个量级下差距能拉到 上千倍。所以"grep 够快"这个前提，只在大约 十万文件 以下的代码库里成立。这就自然引出了下一个问题：既然 grep 有天花板，是不是所有人都该无脑跟着 Anthropic 走？ 转折：Cursor 反着走，而且拿得出数据 我一开始的想法挺简单粗暴：grep 这么香，那向量检索是不是可以扔进历史垃圾桶了？后来认真看了 Cursor 17 的做法，发现事情没这么干脆。同样是头部编码工具，Cursor 反着来，一整套完整的 RAG 管线全上：用 tree-sitter 做语义切块，自己训了个 embedding 模型，存进向量库，再用 Merkle 树做增量同步 18，查询时跑 KNN 加本地取原文。这套索引到底怎么跑的，Towards Data Science 19 有篇讲得很细的文章可以参考。 关键是人家有数据。Cursor 官方博客给的实测结论是这样的： Achieving on average 12.5% higher accuracy in answering questions (6.5%–23.5% depending on the model). 语义搜索平均把问答准确率拉高了 12.5%，不同模型之间在 6.5% 到 23.5% 之间浮动。 不止准确率。他们还测出来，大型代码库（1000 个文件以上）里，加了语义搜索之后代码留存率能提升 2.6%，而没有语义搜索时，用户因为不满意而追问的比例会多出 2.2%。这些都是真金白银的 A/B 数字，不是嘴上说说。 反方阵营里嗓门最大的是 Milvus 20，向量数据库厂商，立场当然不中立，但话糙理不糙。他们直接开怼，大意是 grep 是条把你淹死在无关匹配里的死路，烧 token、拖垮工作流，没有语义理解就像让 AI 蒙着眼睛调试。他们还推了个向量 MCP 插件，号称能把 token 消耗平均压到原来的六成、工具调用次数压到原来的六成四，同时保住检索质量。 不过这里我得留个心眼。这类 token 节省数字，基本都来自厂商自家插件的自测，你得打个折看。而 Cursor 那个 12.5%，来自它自己的 Context Bench 评测集，也是自家的。所以看这类对比，来源是不是第三方独立的，这一栏永远要盯着。 学术那边倒是有相对中立的声音。PwC 团队有篇 arXiv 论文 21 标题就叫《Is Grep All You Need?》，在 LongMemEval 上比 grep 和向量检索，结论是 grep 普遍准确率更高；但它同时强调了一个更要命的点，检索方式本身的影响，远不如 Agent 的整体框架和 tool-calling 方式来得大，同一个模型换个 harness，准确率天花板能差出十几个百分点。Amazon 的另一篇论文 22 更直接，标题叫《Keyword search is all you need》，实测纯 agentic 关键词搜索能摸到传统 RAG 九成以上 的性能，在金融问答那类任务上甚至反超。这两篇都指向同一个意思：检索模式重要，但它上面那层 Agent 怎么编排，可能更重要。 有意思的是，哪怕是重仓向量检索的 Cursor，2025 年 3 月流出的 system prompt 里， grep_search 被标成主力探索工具，要求模型先用宽泛关键词 grep，语义搜索只在概念性查询时补上。换句话说，在代码搜索这个任务上，"精确找已知符号"是更高频、更确定的操作，向量检索解决的是 grep 覆盖不到的那条长尾。Cursor 官方反复强调的也是这个：grep 加语义，两个搭一起效果最好，不是谁替代谁。 掰开看：Claude Code 的检索其实是分层的 聊到这你可能觉得，Claude Code 不就是个 grep 套壳吗？我原来也这么以为，直到看了源码分析才发现，它的检索是分好几层的。这里的信息主要来自泄露的 build 快照分析，像 HarrisonSec、Vadim 的博客 23、还有国内几篇分析 24 都对过，方法论上也能对上 Anthropic 那篇《Building effective agents》 25 讲的思路。我先声明，这类结论基于流传的快照，flag 名字、行号都会漂，细节以你手上的版本为准，稳定的是设计取向，不是具体某行代码。 大致分四层。Layer 0 在会话启动时，先把 CLAUDE.md 和 git status 喂进去，给模型一个项目的初始印象。Layer 1 是并行预取，用更便宜的轻量模型去扫你消息里 @file 这类引用，提前把相关文件捞出来。Layer 2 才是真正的 agentic search，Glob、Grep、Read 三件套在一个循环里被反复调用，这三个工具都是只读的，可以并行跑。Layer 3 是子 Agent 委托，遇到需要大量探索的场景，主模型会 spawn 一个 Explore 子 Agent，用更便宜的 Haiku 去跑、独立上下文、只读、优先并行，搜完只把摘要回传给主模型，省得把一堆中间过程塞爆主上下文。这种主从协调的机制，我在 《拆解 Claude Code》多 Agent 协调那篇 里专门聊过，这里不展开。 这里有个细节我觉得特别值得玩味。HarrisonSec 发现，那个"探索超过 3 次查询就 fork 出子 Agent"的规则，是藏在一个 feature flag 后面的，当时正跟另一套 Fork 架构在做 A/B 测试。也就是说，连 Anthropic 自己都还在持续实验，检索这套设计远没定型。你现在看到的"就是 grep"，只是他们某个阶段的最优解，不是刻在石头上的真理。 上升一层：通用文档那边，RAG 根本没退场 前面全在讲代码。可一旦跨出代码库，故事就得反过来讲了，这也是我为什么开头就强调判断要克制。Anthropic 自己在 2024 年 9 月发过一篇《Introducing Contextual Retrieval》 26，给的是改进版 RAG 的实打实数据。原文结论是： contextual embeddings and contextual BM25 reduced the retrieval failure rate by 67% (5.7% to 1.9%). 上下文 embedding 加上下文 BM25，再叠一层重排，把 top-20 chunk 的检索失败率压到了原来的 三分之一 （从 5.7% 降到 1.9%）。 注意这里面 embedding 和 BM25 关键词是搭配着用的，这本身就是一种 hybrid。更关键的是他们那段分场景的话，写得明明白白：如果你的知识库小于 20 万 token，大概 500 页材料，直接全塞进 prompt 就行，压根不需要 RAG；超过这个规模，才轮到 Contextual Retrieval 上场。 你把这句话跟代码场景那个"成本曲线"放一起看，会发现底层逻辑是通的。都是在问同一个问题：我这个场景的规模和变更频率，到底值不值得我预先花成本去建一套索引？代码库变动频繁、又能实时遍历，那就别建；超大文档库相对稳定、又塞不进窗口，那就得建。变量一样，只是代入的数不一样，答案自然不一样。这方面已经有人整理出长上下文 vs RAG 的生产决策框架 27，把该考虑的变量列得挺全。至于 RAG 本身，与其惦记它死没死，不如看它正在往 agentic retrieval 28 进化，把检索包成 Agent 能反复调用的工具，这条线现在也很热。所以到底选哪条路，不如画个二维图定位一下： 再往上抽一层，这件事的演进脉络也清楚了。从 RAG 是绕开小窗口的无奈之举，到长上下文把窗口撑大，再到 Agent 能力成熟、模型能自主决定搜什么怎么搜，检索这件事的重心一路在挪： 最后落到 hybrid 上一点都不意外。让 grep 做精确符号和字面匹配当高频主力，让 embedding 语义搜索去捞那条概念长尾当补充，这正是 Cursor 的实测和 Anthropic 的 Contextual Retrieval 共同指向的结论：好处是可以叠加的，不是二选一。 收尾：被删掉的那一步，和被高估的默认动作 回到开头那个让我纳闷的决定。Anthropic 把 RAG 从 Claude Code 里删掉，删的其实是一个没被质疑过的默认动作，而非某项具体技术。过去两年，"先上 RAG"变成了肌肉记忆，以至于没人再问一句这一步到底有没有必要。而他们做的，就是老老实实把这个问题重新问了一遍，然后发现：在代码这个场景，答案是不必要。 对我们做技术选型的人来说，这里能带走的东西比 grep 还是 embedding 更值钱。真正的判断力，从来不是记住"代码就用 grep、文档就用 RAG"这种结论，那种东西过两年又会被新的窗口大小、新的模型能力翻案。真正实打实的东西是那套控制变量的思路：把场景的规模、变更频率、查询类型、上下文成本这几个变量单独拎出来，一个个代进去算，而不是抓起行业默认动作就往上套。工具会换，窗口会涨，但"这一步到底有没有必要"这个问题，值得你对着每一个默认动作，都重新问一遍。关于 Claude Code 内部这些设计的更多梳理，都收在我那个 《拆解 Claude Code》专栏 里，想深挖的可以去翻。 参考

最近在看 RAG 的两个新思路，其实也不是说特别新，有点新瓶装旧酒的意思。

对于 RAG 的优化，一种线路是继续优化传统 RAG：切块更细一点，召回器多堆几层，重排再优化，最后把 prompt 拼得更精致。另一条线路是开始换问题定义：如果知识检索本身的建模方式就不对，后面那一长串补丁是不是从第一步就已经输了。

SAG 和 OpenViking 属于第二种。

这两个项目看起来也不在一个层面上。SAG 讨论的是检索架构，OpenViking 讨论的是 Agent 的上下文系统。但我看下来，它们都是在解决同一类问题： **RAG 之所以越来越重，往往不是因为模型不够强，而是因为我们把「知识」和「上下文」这两个对象建模得太糙。**

过去一年，团队在 RAG 上花的钱和精力，主要耗在三个地方：

- 文档切块以后，语义边界被打碎；
- 多跳关联靠 embedding 硬撞，召回经常断链；
- 想补结构化能力，就往 GraphRAG 走，结果离线构图、实体归一、增量更新把系统拖住。

这里的问题是这条路线会把系统带向复杂化。

## SAG

SAG 的核心论文《SAG: SQL-Retrieval Augmented Generation with Query-Time Dynamic Hyperedges》提出了一种非常精妙的架构。它不是传统 RAG 与 GraphRAG 的简单拼接，而是一套用自己的数据模型和执行路径替代二者的原创检索架构。

它的核心思想可以总结为八个字： **轻量离线，动态建图。**

### 1\. 数据模型

传统 GraphRAG 把文本切碎，试图把所有知识都抽象成 `(Subject, Predicate, Object)` 的三元组。这种做法丢失了文本原本的上下文语义，导致最后召回出来的都是一些干瘪的关系链，LLM 很难根据这些碎片生成高质量的回答。

SAG 重新设计了数据模型：

- **Chunk Event（事件）**：一个语义完整的文本块（Chunk）被映射为一个 Event。Event 承载该 Chunk 的完整上下文，它是最终输出和溯源的边界，不再被拆成彼此独立的三元组。
- **Chunk Entities（实体）**：从该 Chunk 中抽取多个 Entities。这些实体 **只负责索引和扩展**，不替代事件本身所承载的完整含义。
- **Event Entities**：建立事件与实体之间的关联，共同定义了一条潜在的超边（Latent Hyperedge）。
```
+-------------------------------------------------------------+
|                         Original Chunk                      |
+-------------------------------------------------------------+
                               |
                               v
+-------------------------------------------------------------+
|                         Event (完整语义)                     |
+-------------------------------------------------------------+
            /                  |                  \
           v                   v                   v
+------------------+   +------------------+   +------------------+
|     Entity A     |   |     Entity B     |   |     Entity C     |
+------------------+   +------------------+   +------------------+
```

这个设计让事件和实体各司其职。实体只用来做连接的「钩子」，而事件负责保留完整的语义。

### 2\. 查询时动态超边

SAG **不预先构建、也不全局维护** 任何静态图谱。所有的关系推理，都是在 **检索发生的那一瞬间，通过关系型数据库的 SQL JOIN 动态计算出来的**。

当一个用户查询（Query）进来时，在线检索流程如下：

1. **种子定位**：通过语义（向量）与词法（全文检索）信号，找到与查询最相关的「种子实体」和「种子事件」。
2. **SQL 动态扩展**：利用关系型数据库的 SQL JOIN 查询，沿着这些种子实体，将共享相同实体的事件连接起来。
3. **局部超边实例化**：在查询时，仅针对当前查询所需的局部结构实例化「超边」，不进行任何全局图遍历。
4. **证据还原**：将筛选出的事件映射回原始 Chunk，去重后作为最强证据提供给 LLM 生成带引用的回答。

在这个过程中，我们不需要 Neo4j，不需要复杂的图算法，底层的存储与查询完全依赖标准的数据库基础设施（如 SQLite、PostgreSQL、LanceDB 等）。

因为超边是查询时动态计算的，所以当有新文档导入时， **不需要重算或重构全局图谱**。新 Chunk 只需要并行抽取自身的 Event、Entities 并写入数据库即可。这让系统天然支持了高并发的增量写入。

从跑分数据来看，在相同的 `BGE-Large-EN-v1.5` Embedding 与 `Qwen3.6-Flash` LLM 配置下，SAG 在 HotpotQA、2WikiMultiHopQA 和 MuSiQue 的 9 项 Recall@1/2/5 指标中取得了 8 项最佳成绩。其平均 Recall@2/Recall@5 达到了 79.30%/88.18%，而 HippoRAG 2 仅为 68.14%/83.28%。

### 一些问题

SAG 不是银弹，还存在一些问题。

抽取质量依然依赖 LLM。Event 的切分和 Entity 的抽取用的还是大模型，模型抽错了，后面的动态连接就是在错误的实体上做 JOIN。SAG 降低了实体的语义负担，但没消除抽取本身的不确定性。这块的鲁棒性得拿自己的语料实测。

查询时的实体提取是另一个隐患。动态超边的起点是从 Query 里提出核心实体，如果用户的提问很口语、实体表达很模糊，种子定位偏了，后面整条链就歪了。这个环节的召回率我认为是整个系统最脆弱的地方，值得单独压测。

还有并发下的延迟分布。JOIN 在数据量大的时候，SQL 的执行计划、索引命中情况会直接影响尾延迟。其底层继承了数据库的并发能力，在海量数据情况下需要观察其延迟分布。

## OpenViking

聊完 SAG，看下 OpenViking。它跟 SAG 不是同一个层面的东西，我把它俩放一篇文章里，是因为它们代表了同一个方向上两种不同深度的探索。

SAG 解决的是「怎么把知识检索得更准」。OpenViking 解决的是「Agent 的上下文该怎么组织」。前者是检索问题，后者是上下文工程问题。

火山引擎把它定位成一款面向 AI Agent 的上下文数据库，基于开源的 OpenViking 内核构建的全托管云服务。核心思想它自己概括成四句：虚拟文件系统、分层上下文、目录递归检索、可观测与自迭代。

### 万物皆文件

OpenViking 把 Memory、Resource、Skill 三样东西统一抽象成文件，全部映射到 `viking://` 协议下的虚拟目录，每个条目有唯一的 URI。

这个抽象是 Agent 发展到现在的一种沉淀，这里最早是 Manus 在实践的逻辑。Agent 领域现在最乱的就是上下文管理。你有用户的长期记忆、有外部知识资源、有工具和技能定义，这三样东西各自用各自的存储、各自的检索方式，代码里到处是特判。OpenViking 把它们收敛成同一套文件语义之后，Agent 就能用 `list`、 `find` 这种统一指令去操作所有上下文。

从模糊的语义匹配变成确定性的文件操作，这个转变对调试很有帮助。向量检索是个黑盒，你不知道为什么这次召回了这几条、漏了那几条。文件系统是白盒，路径、目录结构摆在那，Agent 走了哪条路一目了然。

### 分层加载

OpenViking 在上下文写入的时候，就自动把它处理成三层。

L0 是一句话摘要，用来快速判断。L1 是概述，包含核心信息和使用场景，供 Agent 在规划阶段决策。L2 是完整原始数据，Agent 确有必要时才深入读取。

这个设计针对的是一个很现实的成本问题。把海量上下文一次性塞进提示词，token 烧钱、容易爆窗口、还引入噪声。分层之后，Agent 在规划阶段只看 L0 和 L1 就能做大部分决策，只有真正要用某份资料的时候才去拉 L2。

我算过一笔账。一个复杂的多步 Agent 任务，如果每一步都把候选上下文的全文塞进去，token 消耗是指数级的。分层供给相当于给 Agent 一个「先看目录再翻正文」的能力，规划阶段的 token 成本能压下去一大截。具体压多少取决于你的任务形态，但这个方向省的钱是实打实的。

### 目录递归检索

OpenViking 的检索不是单次向量召回。流程是这样：先通过意图分析生成多个检索条件，用向量检索快速定位初始切片所在的高分目录，然后在这个目录下做二次检索，把高分结果更新到候选集合；如果目录下还有子目录，就逐层递归重复二次检索；最后拿到最相关的上下文返回。

「先锁定高分目录，再精细探索内容」，这套策略把向量检索的语义定位能力和文件系统的层次结构结合起来了。单纯的向量检索找到的是孤立的片段，它不理解片段所在的完整语境。目录递归能顺着文件的组织结构，把一个片段周围的语境一起带出来。

这个思路和 SAG 的动态超边其实有相通之处。两者都意识到孤立的语义片段不够用，都想在检索时把「关联」这层信息补回来。SAG 用的是共享实体的 SQL JOIN，OpenViking 用的是目录层级的递归遍历。一个走关系代数，一个走树形结构。解决的底层痛点是同一个。

### 可观测与自迭代

OpenViking 的检索过程，每次的目录浏览、文件定位轨迹都被完整留存，能看清问题根源、指导检索逻辑优化。

对做过 RAG 调优的人来说，这个能力的分量不用多解释。传统向量检索出了问题，你几乎无从下手，只能瞎调 embedding、瞎调 chunk size。有了完整的检索轨迹，你能精确复盘 Agent 每一次信息获取走了哪条路径，哪一步偏了。

自迭代那部分是通过 `session.commit()` 主动触发的。会话结束时，系统异步分析任务执行结果和用户反馈，自动更新到 User 和 Agent 的 `/memory` 目录下。它既更新用户偏好，也从任务执行经验里提炼操作技巧和工具使用经验。

这个闭环我持谨慎乐观。自动提炼记忆听着很美，但提炼的质量、更新的边界、错误记忆的污染问题，都是需要长期跑才能暴露的。我不会一上来就把它当成生产依赖，会先在低风险场景里观察它更新出来的记忆到底靠不靠谱。

## SAG 和 OpenViking

写到这，我把 SAG 和 OpenViking 放到一起，讲讲我看到的相同点。

传统 RAG 的世界观是扁平的。所有知识被切成等长的 Chunk，拍平进一个向量空间，检索就是在这个空间里找最近邻。这套世界观简单、好扩展，但它主动丢弃了知识之间的结构信息。Chunk 和 Chunk 之间是什么关系，谁包含谁，谁引用谁，谁是谁的上下文，全都被拍平的过程碾掉了。

SAG 和 OpenViking 从两个不同的方向，试图把这层被碾掉的结构信息找回来。

SAG 找回的是「实体关联」这层结构。它承认知识点之间存在通过共享实体建立的隐式关联，并且用查询时的 SQL JOIN 把这层关联即时重建出来。它的贡献在于证明了这层结构不需要离线物化成一张昂贵的全局图，用关系型数据库现成的关联能力就能在查询时高效算出来。

OpenViking 找回的是「层次组织」这层结构。它承认知识天然有目录、有层级、有从属关系，并且用虚拟文件系统把这层组织显式地保留下来，检索的时候顺着这个层级递归下去。它的贡献在于把 Agent 的上下文从一堆散落的切片，重新组织成了一个可导航、可追溯的结构。

两者都在做同一件事：扁平化的向量检索丢失了太多结构，而这些结构恰恰是复杂查询和长程任务真正需要的东西。它们的分歧只在于用什么载体去承载这层结构。SAG 选了关系表和 SQL，OpenViking 选了文件系统和目录树。

## 怎么选

落到实际决策，可以分两个场景。

如果核心诉求是知识库的多跳检索精度，语料相对静态、增量频繁、又要给用户看得见的原文引用，建议评估 SAG。它的动态超边在多跳上的数据摆在那，增量友好和溯源能力恰好解决了之前方案的问题。部署门槛低，本地就能验，评估成本几乎为零。

如果做的是 Agent 产品，痛点在长程任务的上下文管理、记忆沉淀、多 Agent 协作，建议去评估 OpenViking。它解决的是比检索更上一层的问题，文件系统抽象和分层加载对控制 Agent 的 token 成本、提升可调试性比较有用。

这两个东西不冲突。理论上一个成熟的 Agent 系统，底层完全可以用 SAG 这类引擎做精准的知识检索，上层用 OpenViking 这类框架做上下文的组织和调度。一个管「检索得准」，一个管「组织得清」，各司其职。

检索这条路走到今天，值得关注的方向已经从「怎么把向量算得更快」转到了「怎么把结构找回来」。SAG 和 OpenViking 是这个转向上两个具体的答案，一个在检索层，一个在上下文层。

以上。

[刘翔被体育局买断获49.4万 500 万](https://www.zhihu.com/search?q=%E5%88%98%E7%BF%94%E8%A2%AB%E4%BD%93%E8%82%B2%E5%B1%80%E4%B9%B0%E6%96%AD%E8%8E%B749.4%E4%B8%87&search_source=Trending&utm_content=search_hot&utm_medium=organic&utm_source=zhihu&type=content&request_click_id=c9fbece1-ffc4-4634-a504-5223975a0c68) 新

[宁德时代已报警 499 万](https://www.zhihu.com/search?q=%E5%AE%81%E5%BE%B7%E6%97%B6%E4%BB%A3%E5%B7%B2%E6%8A%A5%E8%AD%A6&search_source=Trending&utm_content=search_hot&utm_medium=organic&utm_source=zhihu&type=content&request_click_id=c9fbece1-ffc4-4634-a504-5223975a0c68) 热

[2026 苹果秋季发布会 436 万](https://www.zhihu.com/search?q=2026+%E8%8B%B9%E6%9E%9C%E7%A7%8B%E5%AD%A3%E5%8F%91%E5%B8%83%E4%BC%9A&search_source=Trending&utm_content=search_hot&utm_medium=organic&utm_source=zhihu&type=content&request_click_id=c9fbece1-ffc4-4634-a504-5223975a0c68) 新

[苹果折叠屏iPhone Duo 404 万](https://www.zhihu.com/search?q=%E8%8B%B9%E6%9E%9C%E6%8A%98%E5%8F%A0%E5%B1%8FiPhone+Duo&search_source=Trending&utm_content=search_hot&utm_medium=organic&utm_source=zhihu&type=content&request_click_id=c9fbece1-ffc4-4634-a504-5223975a0c68) 新

[知乎 CLI 创作者能力上新 388 万](https://www.zhihu.com/search?q=%E7%9F%A5%E4%B9%8E+CLI+%E5%88%9B%E4%BD%9C%E8%80%85%E8%83%BD%E5%8A%9B%E4%B8%8A%E6%96%B0&search_source=Trending&utm_content=search_hot&utm_medium=organic&utm_source=zhihu&type=content&request_click_id=c9fbece1-ffc4-4634-a504-5223975a0c68) 热

[教师节 380 万](https://www.zhihu.com/search?q=%E6%95%99%E5%B8%88%E8%8A%82&search_source=Trending&utm_content=search_hot&utm_medium=organic&utm_source=zhihu&type=content&request_click_id=c9fbece1-ffc4-4634-a504-5223975a0c68)

[江西孩子看演唱会后全家低保取消 372 万](https://www.zhihu.com/search?q=%E6%B1%9F%E8%A5%BF%E5%AD%A9%E5%AD%90%E7%9C%8B%E6%BC%94%E5%94%B1%E4%BC%9A%E5%90%8E%E5%85%A8%E5%AE%B6%E4%BD%8E%E4%BF%9D%E5%8F%96%E6%B6%88&search_source=Trending&utm_content=search_hot&utm_medium=organic&utm_source=zhihu&type=content&request_click_id=c9fbece1-ffc4-4634-a504-5223975a0c68) 热

[高考132分学生开学数学考12分 309 万](https://www.zhihu.com/search?q=%E9%AB%98%E8%80%83132%E5%88%86%E5%AD%A6%E7%94%9F%E5%BC%80%E5%AD%A6%E6%95%B0%E5%AD%A6%E8%80%8312%E5%88%86&search_source=Trending&utm_content=search_hot&utm_medium=organic&utm_source=zhihu&type=content&request_click_id=c9fbece1-ffc4-4634-a504-5223975a0c68)

[吃播网红干饭莹莹去世年仅24岁 308 万](https://www.zhihu.com/search?q=%E5%90%83%E6%92%AD%E7%BD%91%E7%BA%A2%E5%B9%B2%E9%A5%AD%E8%8E%B9%E8%8E%B9%E5%8E%BB%E4%B8%96%E5%B9%B4%E4%BB%8524%E5%B2%81&search_source=Trending&utm_content=search_hot&utm_medium=organic&utm_source=zhihu&type=content&request_click_id=c9fbece1-ffc4-4634-a504-5223975a0c68)

[OpenAI 被指学术掠夺 286 万](https://www.zhihu.com/search?q=OpenAI+%E8%A2%AB%E6%8C%87%E5%AD%A6%E6%9C%AF%E6%8E%A0%E5%A4%BA&search_source=Trending&utm_content=search_hot&utm_medium=organic&utm_source=zhihu&type=content&request_click_id=c9fbece1-ffc4-4634-a504-5223975a0c68)